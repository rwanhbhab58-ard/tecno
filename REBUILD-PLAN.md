# خطة إعادة بناء موقع تكنو إنجاز (v3)

> الفرع: `rebuild-v3` · تاريخ الخطة: 2026-09-23
> المنهج: إعادة بناء من الصفر، لا ترقيع. يُنقل **المحتوى الحقيقي والأصول** فقط، ويُعاد كتابة كل سطر من الكود.

---

## 1. تشخيص المشروع القديم

### ما هو الموقع فعلياً؟
«تكنو إنجاز» مكتب هندسي في **حماة – سوريا** (ساحة العاصي، بناء الخاني، الطابق الرابع) يعمل في مسارين واضحين من المحتوى:

1. **المشاريع الهندسية ومشاريع التخرّج**: 21 مشروعاً موثقاً (رؤية حاسوبية، ذكاء اصطناعي، روبوتات، أنظمة تحكم، تطبيقات موبايل وويب) مع ملفات توثيق على Google Drive، و14 صورة لنماذج روبوتية منفذة، و3 فيديوهات حقيقية على YouTube. نموذج التواصل القديم كان يسأل عن «الجامعة» و«الاختصاص»، ما يؤكد أن الجمهور الأساسي **طلاب الهندسة والباحثون**.
2. **تطوير مواقع وأنظمة ويب**: 13 مشروعاً حياً بروابط حقيقية ولقطات شاشة (نظام حساب ERP، ProjectForge، متجر خزامة، Modeya، CableXperts، CableKSA، …).
3. **مدونة معرفية**: 12 مقالاً عربياً طويلاً وعالي الجودة (IoT، 5G، MCP، رؤية حاسوبية، …) مع مصادر وأسئلة شائعة.

### المشكلات الجوهرية
| المجال | المشكلة | الأثر |
|---|---|---|
| **البنية** | تطبيق React SPA واحد (`App.tsx` بحجم 31KB) يبدّل «تبويبات» بالحالة، دون أي روابط حقيقية | محركات البحث ترى صفحة واحدة فارغة؛ لا يمكن مشاركة رابط صفحة أو مقال |
| **SEO** | لا يوجد HTML مُسبق التوليد؛ المقالات تُحمّل كـ Markdown داخل JS؛ الـ sitemap يشير لروابط غير موجودة فعلياً؛ تعارض النطاق (`technoenjaz.com` مقابل `techno-enjaz.com`) | فهرسة ضعيفة جداً رغم جودة المحتوى |
| **الأداء** | شاشة تحميل «صاروخ» تحجب المحتوى، three.js + ogl + gsap + framer-motion + pdf.js + page-flip، صور PNG بحجم 1–2MB (أكثر من 60MB أصول)، تكرار الأصول في `public/` و`src/assets/` و`demo/` | LCP سيئ، استهلاك بيانات كبير (مهم جداً لمستخدمي سوريا) |
| **الوصولية** | عناصر 3D غير قابلة للاستخدام بلوحة المفاتيح، نصوص على صور، تباين ضعيف في الوضع الفاتح، غياب معالم الصفحة | فشل WCAG |
| **المصداقية** | محتوى وهمي: 6 من أعضاء «الفريق» بصور Unsplash وبريد `@company.com` ومدن مختلفة (الرياض، دبي…)، تعليقات وإعجابات ومشاهدات مزيفة، تسجيل دخول وهمي في localStorage، اشتراك نشرة يفتح واتساب، ملف `llms.txt` يذكر أسماء فريق مختلفة تماماً، ونطاق `hisab.technoenjaz.com` غير مستخدم فعلياً | يضرّ الثقة و E-E-A-T، ويضلل نماذج الذكاء الاصطناعي |
| **الكود** | خليط JSX/TSX، مكونات مكررة (`demo.jsx`/`demo.tsx`)، ملفات اختبار puppeteer في الجذر، CSS بحجم مئات الكيلوبايت | صعوبة صيانة |

### ما يُحذف (وهمي أو ضار)
- أعضاء الفريق الستة الوهميون، وكل التعليقات/الإعجابات/المشاهدات المصطنعة.
- تسجيل الدخول والملف الشخصي والمفضلة (لا يوجد خادم؛ كانت محاكاة).
- «ريلز» المشاريع بتعليقاتها وأرقامها المصطنعة (يُحتفظ بالمحتوى الحقيقي للمشاريع الحية).
- شاشة الصاروخ، والقوائم ثلاثية الأبعاد، والمؤثرات الثقيلة، وقارئ PDF ذو الصفحات المقلوبة.
- الادعاءات التقنية غير القابلة للتحقق (Stack مخترع لكل مشروع في `llms.txt`).
- ملفات `*-seo.md` الداخلية لا تُنشر (تبقى في المستودع كمرجع تحريري في `docs/seo-packages/`).

### ما يُحفظ (حقيقي)
- 12 مقالاً كاملاً مع صور الغلاف، الأسئلة الشائعة، والمصادر.
- 21 مشروعاً هندسياً موثقاً بروابط Drive الصحيحة (مع إزالة الروابط المكررة التي كانت placeholders).
- 13 مشروع ويب حي بروابطها ولقطاتها.
- 3 فيديوهات YouTube، 14 صورة نماذج منفذة، 4 صور «لحظات الفريق»، صورة المدير عبد الغني.
- الهوية البصرية (الشعار وألوانه المقاسة في `theme/tokens.css`)، بيانات التواصل والعنوان والإحداثيات.

---

## 2. المعمارية الجديدة

### الاختيار: **Astro (توليد ثابت SSG) + TypeScript صارم**
- كل صفحة = ملف HTML حقيقي مُولَّد مسبقاً بمحتواه الكامل وبياناته الوصفية؛ **صفر JavaScript افتراضياً**.
- JavaScript صغير جداً ومعزول فقط لـ: تبديل الوضع الليلي، القائمة في الجوال، فلترة المشاريع (تحسين تدريجي — كل المحتوى ظاهر دون JS)، ومشغّل YouTube الخفيف (facade).
- **Content Collections** بمخطط Zod للمقالات؛ ملفات بيانات TypeScript مُنمّطة للمشاريع والفيديوهات والفريق والأسئلة الشائعة.
- `astro:assets` + sharp لتوليد AVIF/WebP بأحجام متجاوبة، مع `width/height` لمنع CLS.
- خط **Readex Pro** (خط الهوية القديم) مستضاف ذاتياً ومتغيّر الوزن، مع `font-display: swap`.

### خريطة الروابط (URLs)
| الصفحة | العربية (أساسية) | English |
|---|---|---|
| الرئيسية | `/` | `/en/` |
| الخدمات | `/services/` | `/en/services/` |
| المشاريع الهندسية | `/projects/` | `/en/projects/` |
| مشاريع الويب الحية | `/web-projects/` | `/en/web-projects/` |
| الفيديوهات | `/videos/` | `/en/videos/` |
| المقالات | `/articles/` | `/en/articles/` |
| مقال | `/articles/{slug}/` (12 مقالاً) | — (المقالات عربية فقط؛ صفحة الفهرس الإنجليزية تعرض ملخصاً إنجليزياً وتوضح لغة المقال) |
| من نحن | `/about/` | `/en/about/` |
| الأسئلة الشائعة | `/faq/` | `/en/faq/` |
| تواصل معنا | `/contact/` | `/en/contact/` |
| 404 | `/404.html` | |

روابط المقالات القديمة `/articles/{slug}` محفوظة بنفس الـ slug.

### بنية المجلدات
```
src/
  assets/            صور محسّنة عبر astro:assets
  components/        مكونات Astro صغيرة ذات مسؤولية واحدة
  content/articles/  المقالات (Markdown + frontmatter)
  data/              site.ts, projects.ts, webProjects.ts, videos.ts, team.ts, faq.ts, services.ts
  i18n/              ui.ts (نصوص الواجهة) + utils.ts (مسارات اللغة)
  layouts/           BaseLayout (head/SEO/JSON-LD) + ArticleLayout
  lib/               seo.ts (JSON-LD builders), articles.ts, markdown helpers
  pages/             الصفحات العربية + pages/en/ + sitemap.xml.ts + llms.txt.ts + robots.txt.ts
  styles/            tokens.css, global.css, prose.css
scripts/verify-build.mjs  فحص آلي لمخرجات البناء
```

---

## 3. نظام التصميم

- **الألوان**: مشتقة من الشعار (تركواز `#26BFBE` → أزرق `#4E7FBD` + بنفسجي الدائرة `#5D54F2`). ألوان النص والروابط معايرة لتباين ≥ 4.5:1 في الوضعين.
- **الوضعان**: فاتح وداكن عبر متغيرات CSS، يتبع `prefers-color-scheme` افتراضياً مع زر تبديل يحفظ الاختيار، وسكربت صغير في `<head>` لمنع الوميض.
- **الطباعة**: Readex Pro، مقياس سوائلي بـ `clamp()`، ارتفاع سطر 1.8 للنص العربي الطويل، عرض أقصى للقراءة ~70ch.
- **الاتجاه**: خصائص CSS منطقية (`margin-inline`, `padding-block`, `inset-inline-start`) لتعمل التخطيطات في RTL/LTR دون تكرار.
- **المكونات**: رأس ثابت بسيط، مسار تنقل (Breadcrumb)، بطاقات (مقال/مشروع/مشروع ويب/فيديو)، شارات، أزرار، أكورديون أسئلة (`<details>` أصلي)، دعوة لاتخاذ إجراء، تذييل.
- **الحركة**: انتقالات خفيفة فقط، مع احترام `prefers-reduced-motion`.

---

## 4. استراتيجية SEO

- لكل صفحة: `<title>` فريد، `meta description`، `canonical` مطلق، `hreflang` (ar / en / x-default) للصفحات المترجمة فقط، Open Graph وTwitter Card بصورة 1200×630 مطلقة الرابط، `og:locale` صحيح.
- `lang`/`dir` صحيحان على `<html>` لكل صفحة (لا تغيير لاحق بـ JS).
- هيكل عناوين سليم (H1 واحد لكل صفحة)، روابط داخلية مقصودة: المقالات ↔ المقالات ذات الصلة ↔ الخدمات ↔ المشاريع.
- `sitemap.xml` مولَّد من نفس البيانات (مع `xhtml:link` للبدائل اللغوية و`lastmod` حقيقي)، `robots.txt` يشير إليه.
- النطاق الأساسي قابل للضبط بمتغير `SITE_URL` (افتراضياً `https://technoenjaz.com` لتطابق البريد الرسمي) — لإنهاء تعارض النطاقين.
- المحلي (Local SEO): عنوان كامل، إحداثيات، رابط الخريطة، هاتف قابل للنقر، تطابق NAP في كل الصفحات.

## 5. استراتيجية GEO (تحسين الظهور في محركات الذكاء التوليدي)

- **إجابة أولاً**: كل صفحة تبدأ بفقرة تعريف واضحة تجيب «من/ماذا/أين» قبل التفاصيل.
- **وضوح الكيانات**: اسم المؤسسة بالعربية والإنجليزية، المدينة، الخدمات، الأشخاص — بنفس الصياغة في النص والـ JSON-LD و`llms.txt`.
- **بيانات منظمة** (`@graph` مترابط بـ `@id`): `Organization` + `LocalBusiness/ProfessionalService`، `WebSite`، `WebPage`، `BreadcrumbList`، `Article` (مع author/publisher/image/dates/inLanguage)، `FAQPage` (في صفحة الأسئلة وفي كل مقال من قسم «الأسئلة الشائعة» فيه)، `Person` (عبد الغني)، `ItemList` للمشاريع، `VideoObject` للفيديوهات، `ContactPage`/`AboutPage`/`CollectionPage`.
- **`/llms.txt`** و**`/llms-full.txt`** مولدان من البيانات الحقيقية نفسها (لا نص يدوي قد ينحرف): تعريف، خدمات، روابط كل الصفحات والمقالات مع ملخصاتها، الأسئلة الشائعة.
- ذكر المصادر في المقالات محفوظ (يعزز الموثوقية لدى النماذج).

## 6. استراتيجية الأسئلة الشائعة (FAQ)

- صفحة `/faq/` مستقلة ثنائية اللغة، مجمّعة حسب المحاور: عن المكتب، مشاريع التخرج، تطوير المواقع، الأسعار والمدة، التواصل.
- الإجابات مكتوبة بنمط «إجابة مباشرة في الجملة الأولى» ثم التوضيح.
- مقتطف من الأسئلة الأهم على الرئيسية، وأسئلة مرتبطة في صفحات الخدمات.
- أسئلة كل مقال تُستخرج آلياً من قسم «الأسئلة الشائعة» في Markdown وتُعرض وتُصدَّر كـ `FAQPage`.
- ملاحظة: Google أوقف نتائج FAQ المنسّقة (Rich Results) في 2026 — نُبقي `FAQPage` لقيمته الدلالية لمحركات الذكاء التوليدي ومحركات أخرى، لا كحيلة ظهور.
- لا توجد أسعار أو وعود مخترعة: الإجابات تحيل إلى التواصل لتقدير السعر.

## 7. الأداء

- الهدف: LCP < 2.0s على 4G، CLS < 0.05، INP < 100ms، وزن الصفحة الرئيسية < 300KB.
- صور AVIF/WebP متجاوبة مع `loading="lazy"` لغير المرئي أولاً و`fetchpriority="high"` لصورة LCP.
- لا مكتبات عرض ثقيلة؛ YouTube عبر facade (لا iframe حتى النقر، وعبر `youtube-nocookie`).
- CSS واحد صغير مضمّن حسب الحاجة من Astro، خط واحد متغيّر مع subsetting (عربي + لاتيني).

## 8. الوصولية (WCAG 2.2 AA)

- رابط «تخطَّ إلى المحتوى»، معالم `header/nav/main/footer`، `aria-current` للصفحة الحالية.
- تباين ≥ 4.5:1 للنص و3:1 للعناصر، حلقة تركيز واضحة (`:focus-visible`)، أهداف لمس ≥ 24×24px (2.5.8).
- كل الوظائف متاحة بلوحة المفاتيح؛ القائمة في الجوال بزر `aria-expanded` وإغلاق بـ Esc.
- نصوص بديلة وصفية لكل صورة، وصور زخرفية بـ `alt=""`.
- نموذج التواصل: تسميات مرئية، `autocomplete`، رسائل خطأ نصية مرتبطة بـ `aria-describedby`.
- احترام `prefers-reduced-motion` و`prefers-color-scheme`.

## 9. التواصل (بدون خادم)

الموقع ثابت، لذا نموذج التواصل يولّد رسالة جاهزة ويفتحها في **واتساب** أو **البريد** (بشفافية تامة للمستخدم)، مع روابط مباشرة للهاتف/واتساب/البريد/إنستغرام/فيسبوك وخريطة Google بتحميل كسول.

## 10. التحقق والتسليم

1. `npm run build` = `astro check` (TypeScript صارم) + `astro build`.
2. `npm run verify`: سكربت يفحص كل ملف HTML ناتج: وجود title/description/canonical/og/JSON-LD صالح، hreflang للصفحات المترجمة، H1 واحد، `lang/dir` صحيحان، وتطابق روابط sitemap مع الملفات.
3. تحديث README بطريقة التشغيل والبناء والنشر (Cloudflare Pages / Netlify / أي استضافة ثابتة).
4. Commits منطقية على فرع `rebuild-v3` دون push.
